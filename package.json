{
  "name": "nexus-graphql-boilerplate",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "nexus": "^0.26.1",
    "nexus-plugin-prisma": "^0.17.0"
  },
  "scripts": {
    "generate": "yarn generate:prisma && yarn generate:nexus",
    "generate:prisma": "prisma generate",
    "generate:nexus": "ts-node --transpile-only src/graphql-server/nexus-schema-gen.ts"
  },
  "devDependencies": {
    "ts-node-dev": "^1.0.0-pre.61",
    "typescript": "^4.0.2"
  }
}
